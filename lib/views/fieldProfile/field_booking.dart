import 'package:flutter/material.dart';

class FieldBooking extends StatelessWidget {
  const FieldBooking({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      'Field Booking',
      style: TextStyle(color: Color(0xffF1EED0)),
    ));
  }
}
