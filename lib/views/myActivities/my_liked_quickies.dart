import 'package:flutter/material.dart';

class MyLikedQuickies extends StatelessWidget {
  const MyLikedQuickies({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      'My Liked Quickies',
      style: TextStyle(color: Color(0xffF1EED0)),
    ));
  }
}
