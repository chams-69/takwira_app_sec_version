import 'package:flutter/material.dart';

class ProfileQuickies extends StatelessWidget {
  const ProfileQuickies({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      'Profile Quickies',
      style: TextStyle(color: Color(0xffF1EED0)),
    ));
  }
}
