import 'package:flutter/material.dart';

class PlayerPosts extends StatelessWidget {
  const PlayerPosts({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      'Player Posts',
      style: TextStyle(color: Color(0xffF1EED0)),
    ));
  }
}
