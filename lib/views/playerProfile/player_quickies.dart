import 'package:flutter/material.dart';

class PlayerQuickies extends StatelessWidget {
  const PlayerQuickies({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      'Player Quickies',
      style: TextStyle(color: Color(0xffF1EED0)),
    ));
  }
}
